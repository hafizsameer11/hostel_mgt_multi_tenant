═══════════════════════════════════════════════════════════════════
   MONGODB ATLAS - QUICK SETUP GUIDE (5 MINUTES)
═══════════════════════════════════════════════════════════════════

🌐 STEP 1: Sign Up
------------------
Go to: https://www.mongodb.com/cloud/atlas/register
- Click "Sign up" (or use Google/GitHub)
- It's FREE forever for development!


📦 STEP 2: Create Free Cluster
-------------------------------
- Click "Build a Database"
- Choose: "M0" (FREE)
- Pick any cloud provider (AWS/Google/Azure)
- Pick a region close to you
- Click "Create Cluster" (takes 1-3 minutes)


👤 STEP 3: Create Database User
--------------------------------
- In left sidebar, click "Database Access"
- Click "+ ADD NEW DATABASE USER"
- Username: hoteluser
- Password: Hotel123! (or choose your own - SAVE THIS!)
- Database User Privileges: "Read and write to any database"
- Click "Add User"


🌍 STEP 4: Whitelist IP Address
--------------------------------
- In left sidebar, click "Network Access"
- Click "+ ADD IP ADDRESS"
- Click "ALLOW ACCESS FROM ANYWHERE"
- Click "Confirm"
(Note: For production, use specific IPs. This is fine for development)


🔗 STEP 5: Get Connection String
---------------------------------
- In left sidebar, click "Database"
- Click "Connect" button on your cluster
- Click "Drivers"
- Copy the connection string

It will look like:
mongodb+srv://hoteluser:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority


📝 STEP 6: Update .env File
----------------------------
Open: D:\nodejs projects\hotel_mangment\backend\.env

Replace the entire content with:
DATABASE_URL="mongodb+srv://hoteluser:Hotel123!@cluster0.xxxxx.mongodb.net/hotel_management?retryWrites=true&w=majority"

IMPORTANT:
- Replace <password> with your actual password
- Add "/hotel_management" before the "?"
- Keep everything in quotes


⚙️ STEP 7: Push Database Schema
---------------------------------
In terminal, run:
    npx prisma db push


🚀 STEP 8: Restart Your Server
-------------------------------
Stop your Node.js server (Ctrl+C) and restart it


✅ STEP 9: Test in Postman
---------------------------
Send your POST request to: http://localhost:3000/api/register

Body (JSON):
{
    "name": "Test User",
    "email": "test@example.com",
    "phone": "1234567890",
    "password": "password123"
}


═══════════════════════════════════════════════════════════════════
   EXAMPLE CONNECTION STRING
═══════════════════════════════════════════════════════════════════

Before (in .env):
DATABASE_URL="mongodb://localhost:27017/hotel_management?replicaSet=rs0"

After (in .env):
DATABASE_URL="mongodb+srv://hoteluser:Hotel123!@cluster0.abc123.mongodb.net/hotel_management?retryWrites=true&w=majority"

Make sure to:
✓ Replace "Hotel123!" with YOUR password
✓ Replace "cluster0.abc123" with YOUR cluster URL
✓ Include "/hotel_management" before the "?"

═══════════════════════════════════════════════════════════════════

💡 TROUBLESHOOTING
------------------
If you still get errors:

1. Check .env file has the correct connection string
2. Make sure you added "/hotel_management" in the URL
3. Verify your password doesn't have special characters like @, :, /, ?
   (If it does, URL-encode them)
4. Run: npx prisma generate
5. Run: npx prisma db push
6. Restart your server

═══════════════════════════════════════════════════════════════════

🎉 THAT'S IT! Your API will work with MongoDB Atlas!

═══════════════════════════════════════════════════════════════════


